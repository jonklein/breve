@use Abstract.

@plugin "URLPlugin.o" (slInitURLFunctions).

Abstract : URLReader {
	% The URLReader class can grab files as text.

	+ to get url theURL (string):
		% Returns the text file at the specified URL as a string.

		return getURL(theURL).
}
